<header>
  <a class="logo" href="/">
    <img src="/images/logo-clipped.svg"></img>
    <h1>Gnosjö Missionskyrka</h1>
  </a>

  {% if collections.pages | length %}
    <nav>
      {% for item in collections.pages %}
        <a class="undecorated-link" href="{{ item.url }}">{{ item.data.title }}</a>
      {% endfor %}
    </nav>
  {% endif %}
</header>